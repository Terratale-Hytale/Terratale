$PrimaryTheme = "../PrimaryTheme.ui";

@Heading2Style = $PrimaryTheme.@Heading2Style;
@heading2Padding = $PrimaryTheme.@heading2Padding;

Group {
    LayoutMode: Left;

    Label #Text {
        Padding: (...@heading2Padding);
        Text: "";
        Style: (...@Heading2Style);
    }
}
